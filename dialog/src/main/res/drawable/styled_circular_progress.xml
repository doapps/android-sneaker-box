<?xml version="1.0" encoding="utf-8"?>
<rotate xmlns:android="http://schemas.android.com/apk/res/android"
    android:duration="3000"
    android:fromDegrees="0"
    android:pivotX="50%"
    android:pivotY="50%"
    android:toDegrees="720" >
    <!--
    The rotation speed of the indeterminate progress bar is fixed at one revolution every 4 seconds,
    changing the duration attribute in the drawable XML has no effect.
    If you prefer to speed it up, you can change the toDegrees attribute to multiples of 360:
    720 makes one turn in 2 seconds
    1080 makes one turn in 1.33 seconds
    1440 makes one turn in 1 second
      -->
    <!--
<rotate xmlns:android="http://schemas.android.com/apk/res/android"
    android:fromDegrees="270"
    android:toDegrees="270">

    -->
    <shape
        android:innerRadius="20dp"
        android:shape="ring"
        android:thickness="2dp"
        android:useLevel="false" >
        <size
            android:height="48dp"
            android:width="48dp" />
        <!--
        <shape
            android:innerRadiusRatio="2.5"
            android:shape="ring"
            android:thickness="2dp"
            android:useLevel="true"><!- - this line fixes the issue for lollipop api 21 -->

        <gradient
            android:centerColor="#237acc"
            android:centerY="0.5"
            android:endColor="#2366e2"
            android:startColor="#002a88ec"
            android:type="sweep"
            android:useLevel="false" />
        <!--  <gradient
             android:angle="0"
             android:endColor="#007DD6"
             android:startColor="#67a5d1"
             android:type="sweep"
             android:useLevel="false" />
         -->
        <!--
        <gradient
            android:centerColor="#80ec7e2a"
            android:centerY="0.5"
            android:endColor="#ffec7e2a"
            android:startColor="#00ec7e2a"
            android:type="sweep"
            android:useLevel="false" />
        -->
    </shape>
</rotate>